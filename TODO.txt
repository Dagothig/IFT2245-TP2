cleanup

tous les tests
    exit ne devrait pas attendre que les commandes en background terminent (voir bash, pas explicite dans l'énoncé?)
samples_two
    ordre entre printsen2e et le ls n'est pas garanti
samples_three
    ordre entre imprimeendeuxieme et cettecommandenexistepas n'est pas garanti
samples_four
    ordre entre les blocker et r2(block) n'est pas garanti
samples_five
    ordre entre première ligne de commande et 2e n'est pas garanti
fN et les requêtes qui sont invalides, ex:
    echo banana && f2(echo) && r1(echo protato)
    vu qu'on peut à l'avance établir que le f2 est invalide, est-ce que le echo banana devrait être exécuté?
parse_first_line appelé par run_shell, quel message lorsque ligne entrée invalide?
